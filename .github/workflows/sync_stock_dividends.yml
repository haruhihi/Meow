name: Sync stock dividends

on:
  schedule:
    - cron: '40 20 * * *'
  workflow_dispatch:
    inputs:
      symbols:
        description: 'Optional stock symbols, separated by spaces. Empty means held symbols plus configured watchlist.'
        required: false
        default: ''
      replace_source:
        description: 'Delete old non-Tushare dividend events for each successfully fetched symbol.'
        required: false
        default: 'true'

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'

      - name: Check database secret
        env:
          DATABASE: ${{ secrets.DATABASE }}
        run: |
          if [ -z "$DATABASE" ]; then
            echo "Missing GitHub secret: DATABASE"
            echo "Add it in Settings -> Secrets and variables -> Actions -> Repository secrets."
            exit 1
          fi

      - name: Check Tushare token
        id: tushare-token
        env:
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
        run: node scripts/check-tushare-token.mjs

      - name: Install dependencies
        if: steps.tushare-token.outputs.valid == 'true'
        env:
          DATABASE: ${{ secrets.DATABASE }}
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
        run: |
          printf 'DATABASE=%s\n' "$DATABASE" >> .env
          printf 'TUSHARE_TOKEN=%s\n' "$TUSHARE_TOKEN" >> .env
          npm install

      - name: Sync dividends
        if: steps.tushare-token.outputs.valid == 'true'
        env:
          DATABASE: ${{ secrets.DATABASE }}
          SYMBOLS: ${{ github.event.inputs.symbols }}
          REPLACE_SOURCE: ${{ github.event.inputs.replace_source }}
        run: |
          args=(--sleep 300)
          if [ "${REPLACE_SOURCE:-true}" = "true" ]; then
            args+=(--replace-source)
          fi
          if [ -n "$SYMBOLS" ]; then
            args+=(--symbols $SYMBOLS)
          fi
          node scripts/sync-stock-dividends.mjs "${args[@]}"
